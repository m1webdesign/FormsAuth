@model System.Collections.Generic.List<MB.Models.POCO.Bus>

@foreach(var bus in Model)
{

                @Html.ActionLink("MakeBooking", "MakeBooking", new{busid = bus.ID}, new { @class = "editLink" })
 
    @bus.MakeOfBus
    <BR />
}